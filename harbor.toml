# Deprecated: Harbor only accepts YAML/JSON configs.
# Use harbor.yaml instead:
#   uv run harbor run -c harbor.yaml --ae GEMINI_API_KEY=$GEMINI_API_KEY

[agent]
import_path = "harbor_agent:NanoTerminalAgent"

[job]
dataset = "terminal-bench@2.0"
n_tasks = 3
n_concurrent = 1
