# Higher Gemini + hardened — 1 TB task smoke for compare
n_concurrent_trials: 1
agents:
  - import_path: harbor_agent:NanoTerminalAgent
    kwargs:
      max_turns: 80
datasets:
  - name: terminal-bench
    version: "2.0"
    n_tasks: 1
