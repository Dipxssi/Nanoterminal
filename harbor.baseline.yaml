# Flash baseline — 1 TB task, legacy scaffold (no harden/thinking)
n_concurrent_trials: 1
agents:
  - import_path: harbor_agent:NanoTerminalAgent
    kwargs:
      max_turns: 40
datasets:
  - name: terminal-bench
    version: "2.0"
    n_tasks: 1
